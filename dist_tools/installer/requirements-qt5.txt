# Зависимости для Windows 7 и старше — Qt6 там в принципе не запускается
# (минимум для Qt6 — Windows 10), поэтому ставим PyQt5; signals/qtcompat.py
# подменяет им PyQt6 в sys.modules прозрачно для остального кода.
# Версия зафиксирована — 5.15.x последняя ветка с поддержкой Windows 7
# и одновременно поддерживающая «scoped»-стиль перечислений (Qt.AlignmentFlag.*),
# на который рассчитан остальной код (см. signals/qtcompat.py).
PyQt5==5.15.10
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1

# Ленивые импорты приборных плагинов (signals/contrib), см. requirements-qt6.txt.
# ВАЖНО: tm_devices — относительно новый пакет Tektronix, и НЕ проверено,
# ставится ли он вообще под Python 3.8 (потолок для Windows 7, см. detect.py).
# Если pip откажет именно на tm_devices — закомментировать эту строку: Tektronix
# на лекгаси-системах будет недоступен, но Rigol (голый pyvisa) продолжит работать.
tm_devices>=2.0
pyvisa>=1.13
